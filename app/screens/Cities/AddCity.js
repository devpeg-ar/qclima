import React from "react";
import { StyleSheet, Text, View } from "react-native";
import Form from "../../components/FormAddCity";

export default function AddCity() {
  return (
    <View>
      <Form />
    </View>
  );
}

const styles = StyleSheet.create({});
